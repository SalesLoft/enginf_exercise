- type: serial
  name: deployapp
  service: weatherweb
  steps:
  - command: mix deps.get
  - command: mix do compile
#- name: compile
#  service: weatherweb
#  command: mix do compile
#- name: execute
#  service: weatherweb
#  command: mix phx.server
